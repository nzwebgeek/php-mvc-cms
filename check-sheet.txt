1)
 stage-three-mvc/
│
├── app/
├── bootstrap/
├── config/
├── public/
├── routes/
├── storage/
├── vendor/
├── .gitignore
├── composer.json
└── composer.lock

2) 
Extended:
stage-three-mvc/
│
├── app/
│   ├── Controllers/
│   ├── Models/
│   ├── Views/
│   ├── Core/
│   ├── Helpers/
│   └── Middleware/
│
├── bootstrap/
│
├── config/
│
├── database/
│   ├── migrations/
│   └── seeders/
│
├── public/
│   ├── css/
│   ├── js/
│   ├── images/
│   ├── uploads/
│   └── index.php
│
├── routes/
│   └── web.php
│
├── storage/
│   ├── logs/
│   ├── cache/
│   └── uploads/
│
├── vendor/
│
├── composer.json
└── .env

3) Refactored public/index.php,routes/web.php
moved defined routes from index.php to routes/web.php
so index.php just becomes a front Controllers

Suggested next milestone

At this point, your core architecture is shaping up well:

✅ public/index.php is just the front controller.
✅ routes/web.php contains route definitions.
✅ Router.php handles dispatching.
✅ A Container resolves controllers.

4) bootstrap/app.php , autoload.php done
Each file now has a single responsibility:

public/index.php: Boot the application and dispatch the request.
bootstrap/app.php: Configure services and build the dependency container.
routes/web.php: Register routes.
Router.php: Match the request to a controller.
Controllers: Coordinate the request.
Services: Implement business logic.
Repositories: Handle database access.

5) We've already verified:

✅ Front controller (public/index.php)
✅ Route definitions (routes/web.php)
✅ Router (Router.php)
✅ Bootstrap (bootstrap/app.php)
✅ Configuration (config/config.php)
next Container.php
6) DashboardController, Repository.php cleaned up, 
in side Databse.php made changes moved Mailer in Core/
Looked in PageController.php
added to use App\Repositories\PageRepository; 

7) SettingRepository.php checked was ok then checked Page.php ok
then Core/Model.php seems OK and PageRepository.

8) Checn AuthService ok, then PasswordResetService.php ok, Mailer.php
✅ PageRepository.php is correct
✅ Composer autoloading is correct
✅ PageRepository is registered in the container
✅ Database injection into the repository works

So the problem is not PageRepository.

9) Check Container found unexpected throw and make ammendments ran this command composer dump-autoload

home.php is the issue, Controller is fine

So our immediate checklist is:
✅ AuthService::register() creates the user.
✅ Mailer is injected into AuthService.
✅ bootstrap/app.php creates the Mailer using:
$mailer = new Mailer(
    $config['app']['url'],
    $config['mail']['from']
);

10) The MVC Flow for login:
AuthController
 └── receives request

AuthService
 └── validates credentials
 └── creates login session

UserRepository
 └── finds user from database

View
 └── displays messages only